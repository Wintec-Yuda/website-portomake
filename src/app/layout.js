import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Portomake - Buat Portfolio Online Profesional Cepat & Mudah",
  description:
    "Portomake membantu Anda membuat portfolio online yang profesional, responsif, dan siap pakai hanya dalam 1-2 jam. Termasuk domain, hosting, dan email profesional.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
